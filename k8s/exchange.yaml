apiVersion: v1
kind: Service
metadata:
  name: exchange
  namespace: market-maker
spec:
  selector:
    app: exchange
  ports:
    - { name: http, port: 8080, targetPort: 8080 }
    - { name: hz,   port: 5701, targetPort: 5701 }
---
apiVersion: v1
kind: Service
metadata:
  name: exchange-np
  namespace: market-maker
spec:
  type: NodePort
  selector:
    app: exchange
  ports:
    - { name: http, port: 8080, targetPort: 8080, nodePort: 30181 }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: exchange
  namespace: market-maker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: exchange
  template:
    metadata:
      labels:
        app: exchange
    spec:
      containers:
        - name: app
          image: market-maker:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - { containerPort: 8080 }
            - { containerPort: 5701 }
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: exchange
            - name: HAZELCAST_MEMBERS
              valueFrom:
                configMapKeyRef:
                  name: hazelcast-members
                  key: members
          readinessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 20
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 60
            periodSeconds: 10